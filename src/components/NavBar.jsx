import NavLinks from "./NavLinks";

const NavBar = () => {
  return (
    <nav className="px-10 font-bold border-b navbar bg-base-100 border-b-neutral">
      <NavLinks />
    </nav>
  );
};

export default NavBar;
